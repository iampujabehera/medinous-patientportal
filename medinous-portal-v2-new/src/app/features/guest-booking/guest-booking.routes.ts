import { Routes } from '@angular/router';
import { GuestBookingComponent } from './guest-booking.component';

export const GUEST_BOOKING_ROUTES: Routes = [
  { path: '', component: GuestBookingComponent }
];
